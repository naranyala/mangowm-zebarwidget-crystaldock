# Autorun programs — one command per line
# Lines starting with # are comments, empty lines are ignored
# Format: command [args...]
# Use "daemon:" prefix for programs that should only run once (skip if already running)
# Manage with: autorun-manager.sh (add/remove/enable/disable/list/status)

ocws-welcome
contour
daemon:flameshot --daemon
daemon:dms run
